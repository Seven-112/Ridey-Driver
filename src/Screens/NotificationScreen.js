import React from 'react';
import Notification from '../Components/Notification';

const NotificationScreen = (props) => {
    return <Notification navigation={props.navigation} />
}

export default NotificationScreen