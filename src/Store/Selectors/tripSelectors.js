export const getTripErrors = (state)=>state.trip.error;
export const getTripData = (state)=>state.trip.tripData;
export const getTripStatus = (state)=>state.trip.success;
export const getCurrentTrip = (state)=>state.trip.currentTrip;
